diff --git a/node_modules/@firebase/app/dist/esm/index.esm2017.js b/node_modules/@firebase/app/dist/esm/index.esm2017.js
index 02baf18..d3c2874 100644
--- a/node_modules/@firebase/app/dist/esm/index.esm2017.js
+++ b/node_modules/@firebase/app/dist/esm/index.esm2017.js
@@ -712,6 +712,11 @@ class HeartbeatServiceImpl {
         if (this._heartbeatsCache === null) {
             this._heartbeatsCache = await this._heartbeatsCachePromise;
         }
+        // React Native has no IndexedDB, so the heartbeat storage read resolves to undefined rather than null and the === null guard misses it. Bail out rather than
+        // throwing inside an unawaited promise.
+        if (!this._heartbeatsCache || !this._heartbeatsCache.heartbeats) {
+            return;
+        }
         // Do not store a heartbeat if one is already stored for this day
         // or if a header has already been sent today.
         if (this._heartbeatsCache.lastSentHeartbeatDate === date ||
@@ -742,7 +747,8 @@ class HeartbeatServiceImpl {
             await this._heartbeatsCachePromise;
         }
         // If it's still null or the array is empty, there is no data to send.
-        if (this._heartbeatsCache === null ||
+        if (!this._heartbeatsCache ||
+            !this._heartbeatsCache.heartbeats ||
             this._heartbeatsCache.heartbeats.length === 0) {
             return '';
         }
diff --git a/node_modules/@firebase/app/dist/esm/index.esm5.js b/node_modules/@firebase/app/dist/esm/index.esm5.js
index 4385f95..887e0e3 100644
--- a/node_modules/@firebase/app/dist/esm/index.esm5.js
+++ b/node_modules/@firebase/app/dist/esm/index.esm5.js
@@ -810,6 +810,11 @@ var HeartbeatServiceImpl = /** @class */ (function () {
                         _a._heartbeatsCache = _b.sent();
                         _b.label = 2;
                     case 2:
+                        // React Native has no IndexedDB, so the heartbeat storage read resolves to undefined rather than null and the === null guard misses it. Bail out rather than
+                        // throwing inside an unawaited promise.
+                        if (!this._heartbeatsCache || !this._heartbeatsCache.heartbeats) {
+                            return [2 /*return*/];
+                        }
                         // Do not store a heartbeat if one is already stored for this day
                         // or if a header has already been sent today.
                         if (this._heartbeatsCache.lastSentHeartbeatDate === date ||
@@ -851,7 +856,8 @@ var HeartbeatServiceImpl = /** @class */ (function () {
                         _b.label = 2;
                     case 2:
                         // If it's still null or the array is empty, there is no data to send.
-                        if (this._heartbeatsCache === null ||
+                        if (!this._heartbeatsCache ||
+                            !this._heartbeatsCache.heartbeats ||
                             this._heartbeatsCache.heartbeats.length === 0) {
                             return [2 /*return*/, ''];
                         }
diff --git a/node_modules/@firebase/app/dist/index.cjs.js b/node_modules/@firebase/app/dist/index.cjs.js
index d857e7e..ce5e1f3 100644
--- a/node_modules/@firebase/app/dist/index.cjs.js
+++ b/node_modules/@firebase/app/dist/index.cjs.js
@@ -813,6 +813,11 @@ var HeartbeatServiceImpl = /** @class */ (function () {
                         _a._heartbeatsCache = _b.sent();
                         _b.label = 2;
                     case 2:
+                        // React Native has no IndexedDB, so the heartbeat storage read resolves to undefined rather than null and the === null guard misses it. Bail out rather than
+                        // throwing inside an unawaited promise.
+                        if (!this._heartbeatsCache || !this._heartbeatsCache.heartbeats) {
+                            return [2 /*return*/];
+                        }
                         // Do not store a heartbeat if one is already stored for this day
                         // or if a header has already been sent today.
                         if (this._heartbeatsCache.lastSentHeartbeatDate === date ||
@@ -854,7 +859,8 @@ var HeartbeatServiceImpl = /** @class */ (function () {
                         _b.label = 2;
                     case 2:
                         // If it's still null or the array is empty, there is no data to send.
-                        if (this._heartbeatsCache === null ||
+                        if (!this._heartbeatsCache ||
+                            !this._heartbeatsCache.heartbeats ||
                             this._heartbeatsCache.heartbeats.length === 0) {
                             return [2 /*return*/, ''];
                         }
